# Changelog

## 0.7.0

- Scene 导入改为分层运行时，保留最多 64 个图层及原始顺序、位置、缩放、旋转、透明度和混合模式
- 递归展开父子图层变换与可见性，并使用固定比例场景画布避免不同窗口比例导致人物部件错位
- 新增 Canvas 粒子运行时，读取点、盒、球形发射器及数量、寿命、尺寸、速度、颜色、拖尾和湍流参数
- 将水波、植被摆动、流动、震动、视差、反射与透明度等常见效果映射为轻量动态
- Puppet 保留人物分层并提供动态近似；文字、任意 SceneScript 和未知自定义着色器明确跳过

## 0.6.3

- Scene 工程改为先稳定解包，再按需转换主体纹理，单个异常纹理不再导致整个导入失败
- 避免转换插件不会使用的粒子、按钮及特效纹理，并改善大型工程的 RePKG 输出处理

## 0.6.2

- 设置变化后改为提示用户确认，仅在选择“应用并重启”时重新注入并重载窗口
- 合并短时间内的连续设置修改，避免编辑数值时频繁触发提示

## 0.6.1

- 修改壁纸透明度或视频播放速度后自动重新应用并重载 VS Code 窗口
- 自动迁移旧版 `dynamicWallpaper.opacity` 设置，修复透明度配置被忽略的问题

## 0.6.0

- 新增全局壁纸可见强度和视频播放速度设置，默认跟随工程且不修改原始 `wallpaper.json`
- Wallpaper Engine 导入结果默认写入扩展 `globalStorageUri` 下的受管壁纸库，不再写到 Steam 源工程旁边
- 以 Workshop ID 或标题与源路径哈希生成稳定且防冲突的库条目 ID，并用 `library.json` 记录本地索引
- 当前受管壁纸只在扩展全局状态中保存条目 ID，所有本机路径配置均声明为 machine scope
- 新增选择、打开、导出和删除已导入壁纸命令；删除当前壁纸前会先恢复 Workbench
- 支持通过 `dynamicWallpaper.libraryDirectory` 使用用户指定的绝对路径作为永久壁纸库

## 0.5.1

- 从 VSIX 排除壁纸工程、生成的渲染模板和转换媒体，并增加发布文件清单的强制校验
- 移除特定 Workshop 的内置渲染模板依赖，Scene 仅从用户选择的本地工程进行通用转换
- 打包流程不再生成或附带独立壁纸 ZIP

## 0.5.0

- 将扩展入口、命令用例、领域模型、壁纸工程、导入器及平台实现拆分为清晰的分层目录
- 将 VS Code 命令按项目、Wallpaper Engine 导入、性能和 GPU 职责拆分，入口仅负责装配
- 将发布脚本与 Wallpaper Engine 资源脚本分类整理，并补充架构说明
- VSIX 现在标记为 Windows x64/UI Preview 扩展，并通过标准 `vscode:prepublish` 钩子验证
- 保持原有命令、配置、壁纸工程格式和 Workbench 注入行为兼容

## 0.4.3

- 持久记录动态壁纸的启用状态，VS Code 更新清除 Workbench 注入后会自动恢复
- 主动执行“禁用并恢复 Workbench”后不会在后续启动时被错误地重新启用
- 旧版本升级且注入已经丢失时先询问是否恢复，避免猜测用户此前的启用意图

## 0.4.2

- 打开 PDF 时，中央编辑器区域会像图片和视频预览一样恢复为完全不透明，避免动态壁纸干扰阅读

## 0.4.1

- Scene 导入由严格白名单改为“尽可能转换”，渲染器不支持的内容不再阻止整个工程
- 自动识别 Scene 模型、材质与 RePKG 转换出的全屏视频或图片资源
- 保留可直接渲染的主体媒体，忽略粒子、音频响应、脚本文字和未知着色器并写入转换报告
- 验证 Workshop 3351072238 可提取为 3840×2160、60 FPS 的 H.264 动态主画面

## 0.4.0

- 新增插件内置的 Wallpaper Engine 工程导入命令，无需单独操作 CLI
- 选择源工程后默认在同级创建“原目录名-dynamic-wallpaper”转换工程
- Web 与 Video 工程可通用转换，Scene 工程由随 VSIX 分发的 RePKG 自动解包
- 覆盖目标前使用临时目录转换并完整验证，失败时保留已有转换结果
- 转换成功后自动设为当前壁纸工程，并提供应用、打开目录和查看报告操作
- 对尚未验证的 Scene 明确停止并报告兼容性，不再生成画面错误的近似结果

## 0.3.1

- 修复首个动画帧时间戳回退时产生负帧索引，导致全部 Puppet 图层停止渲染的问题
- 修复 Wallpaper Engine Puppet 顶点 UV 与 WebGL 纹理原点相反，导致女孩和猫仅显示零散区域的问题
- Puppet 蒙皮改为轻量 CPU 顶点流更新，避免不同 Chromium/ANGLE 实现的骨骼矩阵数组兼容问题
- 使用 Microsoft Edge 实际加载三档自包含 HTML，并通过截图与 DevTools 控制台核对画面

## 0.3.0

- 项目许可证由 MIT 切换为 GPL-2.0-only，并补充 Open Wallpaper Engine 来源说明
- 新增 Wallpaper Engine MDLV0016、MDLS2、MDLA3 构建时解析器
- `Imagine [+Media Integration]` 改用 WebGL 2 渲染原始 Puppet 网格、骨骼动画和动画层混合
- 按原始参数和遮罩实现 Water Flow、Water Waves、Water Ripple、Foliage Sway、Pulse 与 Shake
- WebGL 时间轴响应插件暂停消息，切出 VS Code 时冻结、返回时连续恢复
- 三档自包含 HTML 现在内嵌 Puppet、遮罩和效果纹理
- 打包同时在 `build` 生成 VSIX 和可直接解压使用的壁纸 ZIP

## 0.2.6

- 修复新版 VS Code 拒绝从安装目录外加载 Web 壁纸 HTML 的问题
- 应用时将 Web 工程按内容哈希暂存到 Workbench 资源目录
- Web 工程内的 CSS、JavaScript 和图片现在可以通过相对路径正常加载
- Web 资源变化会自动生成新的缓存目录，恢复时清理本插件的暂存资源

## 0.2.5

- 修复窗口焦点事件竞态导致切出 VS Code 后视频偶尔继续播放的问题
- 修复返回 VS Code 后视频偶尔无法恢复的问题
- 增加焦点、可见性、页面生命周期和定时状态校准
- 阻止视频加载及自动播放事件绕过后台暂停状态
- 将暂停与恢复状态同步给 Web 图层，便于其中的动画主动停止
- 统一将 VSIX 发布物输出到 `build` 文件夹

## 0.2.4

- 修复长时间切换到其他应用后暂停画面突然变成渲染器底色的问题
- 卸载视频解码器前冻结最后一帧，返回时等新首帧就绪后再无缝恢复
- 静态图片和 Web 图层不再因后台挂起而被清空

## 0.2.3

- 修复仅更新 VSIX 后 Workbench 仍继续加载旧版注入脚本的问题
- 启动时检测已启用但过期的壁纸注入，并自动迁移到当前渲染器
- 只有旧注入已启用时才自动更新，不会未经用户操作启用壁纸

## 0.2.2

- 查看图片或视频时，中央编辑器区域自动恢复为完全不透明
- 仅匹配图片和视频扩展名，不影响 PDF、音频、代码及普通文本
- 支持多编辑器组、标签切换和布局尺寸变化
- 新增 `render.opaqueEditorForMedia` 开关，默认启用

## 0.2.1

- 新增 `quality`、`balanced`、`economy` 三档性能配置
- 图层支持通过 `sources` 为不同性能档指定不同媒体文件
- 默认在 VS Code 失焦时立即暂停，持续失焦后卸载媒体并释放解码资源
- 省电档禁用视差、动态渐变、颗粒与扫描线，均衡档停止颗粒动画
- 避免无效果的 CSS 滤镜和不必要的图层合成提示
- Blue Archive 示例默认改用 1080p/30fps，并提供 720p/24fps 省电源

## 0.2.0

- 核心改为独立的多图层动态壁纸渲染器
- 移除 Steam、Wallpaper Engine 和 Workshop 扫描
- 使用用户指定文件夹中的 `wallpaper.json`
- 支持视频、图片、动态图片、Web 和动态渐变图层
- 支持图层混合模式、位置、缩放、旋转、视差和完整颜色滤镜
- 支持全局遮罩、暗角、颗粒及扫描线
- 提供示例工程生成命令和 JSON Schema 补全
- 保留内容哈希缓存规避、Workbench 增量恢复和独显偏好

## 0.1.2

- 修复更换壁纸后 Electron 仍加载旧注入脚本的问题
- 注入脚本改用 SHA-256 内容哈希文件名，壁纸或设置变化时 URL 必然更新
- 应用和恢复时自动清理 0.1.0/0.1.1 固定文件及过期哈希文件
- 提升前景混合层的层叠级别，确保已打开文件的 Monaco 编辑器也能显示背景

## 0.1.1

- 默认启用覆盖整个 Workbench 的前景混合渲染模式
- 新增 `dynamicWallpaper.renderLayer` 的 `front` / `behind` 选项
- 补充 Monaco `overflow-guard`、编辑器实例和滚动层透明选择器
- 前景模式按界面底色自动降低等效视频透明度，避免遮挡代码

## 0.1.0

- 首个 Windows MVP
- Wallpaper Engine Workshop 自动扫描
- Video 和基础 Web 壁纸支持
- 全窗口 Workbench 背景注入
- 安全的增量补丁、备份和恢复
- Windows 高性能 GPU 首选项及原值恢复
