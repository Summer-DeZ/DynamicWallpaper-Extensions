# Changelog

## 1.0.7

- 新增可搜索的壁纸库切换器；选择项目后会直接应用并仅重载一次窗口。
- 批量导入 Wallpaper Engine 项目时不再隐式替换当前壁纸，可选择继续导入或立即切换。

## 1.0.6

- 静态 Scene、静态特效、视差与 Web 图层改为按需渲染，视频仅在新解码帧到达时提交 GPU。
- 减少全局后处理和图层效果链的离屏缓冲、重复清屏及无效 Shader 运算，并缓存未变化的效果输出。
- 降低粒子动态属性上传与透明片元开销；隐藏、透明及安全离屏节点不再继续更新或绘制。

## 1.0.5

- 修复 Electron 漏发窗口失焦或聚焦事件时，壁纸暂停/恢复状态不能及时同步的问题；轻量焦点监测可在 200ms 内自动校正。
- 修复快速切换窗口时旧异步回调反向播放，以及媒体或 AudioContext 首次恢复失败后必须再次切换窗口才能播放的问题。
- 普通失焦只暂停渲染并保留 GPU/媒体资源，最小化或真正隐藏时才延迟卸载，避免切回窗口时发生不必要的冷启动。

## 1.0.4

- 修复失焦事件、原生 RAF 或 WebGL context 恢复异常时壁纸可能永久停止的问题，并为自动重建增加退避与熔断保护。
- 暂停时停止 RAF、定时器、CSS 动画、媒体与音频上下文；延迟挂起后释放 GPU、视频及 Web 资源，恢复时可靠重建。
- 优化 Scene、粒子、Timeline、Puppet、后处理及静态壁纸渲染路径，并缩减运行时发布包体积。
- 修复多窗口状态与壁纸库并发写入、旧自定义库升级和导出工程绝对资源 URI 等数据可靠性问题。

## 1.0.3

- PDF 与常见图片编辑器自动使用不透明背景，并支持在设置中自定义触发的文件扩展名。
- 新增默认开启的 `Pause When Unfocused` 扩展设置；切换到其他应用或隐藏 VS Code 时可暂停全部壁纸运行时，重新聚焦后自动恢复。

## 1.0.2

- 卸载扩展时自动删除受管的 Wallpaper Engine 转换壁纸库和运行时状态。
- 壁纸库被手动清除后自动移除失效选择和 Workbench 残留注入，不再反复报告 `ENOENT`。
- 修复 Scene 子节点重复应用场景中心偏移，以及 TEX 动态纹理转换为 MP4 后显示故障纹理的问题。
- 新增 15–60 FPS 全局限制，并停止隐藏视频、隐藏 Pass、暂停状态及空后处理的无效 GPU 提交。
- Scene 启动改为首屏背景优先，素材、效果与 SceneScript 在首帧后按空闲时间分批加载。
- SceneScript 每帧跨沙箱调用由逐绑定执行改为单次批处理，并跳过未变化属性和完全透明节点。

## 1.0.0

- 使用 Three.js WebGL2 统一渲染原生、Wallpaper Engine Scene 与 Video 工程。
- 新增无损 Scene IR、材质多 Pass、Timeline、Puppet、粒子、QuickJS SceneScript 和自有音频分析子系统。
- Web 工程使用二级 sandbox、默认禁网 CSP 和按壁纸域名授权。
- 新增实时用户属性、兼容性报告、运行时诊断以及 WebGL context-loss 自动重建。
- 新增 VS Code 正式卸载钩子，重启后自动清除 Workbench 注入和运行时资源。
- 独立运行时使用 esbuild 生成带内容哈希的资源，并加入 Vitest 与 Playwright 发布检查。
